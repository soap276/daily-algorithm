## 🗓 2026-01-21

### 📌 문제 요구사항
many_user 변수에 신규 고객 정보 딕셔너리를 요소로 갖는 리스트 할당
decrease_book 함수 작성: 전체 책의 수(number_of_book)를 인자로 받은 수만큼 줄임
rental_book 함수 작성: 단 하나의 인자 info를 받음. info 인자로는 {'고객이름': 고객나이} 형태의 딕셔너리 전달.
                      함수 내부에서 info 딕셔너리에서 고객의 이름과 나이 추출. 
                      추출한 나이를 10으로 나눈 몫으로 대여할 책의 수를 계산하고, decrease_book 함수 호출
                      대여 완료 메시지 출력
데이터 형변환 (for문 사용): many_user와 rental_book이 요구하는 데이터 형태가 다르므로 {'name':'김시습'} 형태를 {'김시습':20} 형태로 변환하는 규칙 정의
                         변환된 딕셔너리를 저장할 새로운 빈 리스트 user_info 생성
                         many_user 리스트를 순회하는 for 반복문 작성
                         반복문 내부에서 many_user의 각 고객 딕셔너리로부터 {'이름': 나이} 형태의 신규 딕셔너리를 만든 후 user_info 리스트에 추가
전체 로직 실행(for문 사용): 생성된 user_info를 순회하는 for 반복문 작성. 목록의 각 고객 정보(info)를 rental_book 함수의 인자로 전달하여 호출

---

### 💡 구현 아이디어


---

### 🧠 배운 점
- `global` 키워드를 사용해야 함수 안에서 전역 변수를 수정할 수 있다.
- 딕셔너리에서 key, value는 `keys()`, `values()`로 접근할 수 있다.
- `zip()`을 사용하면 여러 리스트를 깔끔하게 한 번에 순회할 수 있다.
- 기능별로 함수를 나누면 코드가 읽기 쉬워진다.
- append: 추가 함수

---

### 💻 코드

```python
number_of_people = 0
number_of_book = 100

# 책 감소 함수
def decrease_book(rental):
    global number_of_book
    number_of_book -= rental
    print(f'남은 책의 수: {number_of_book}')

# 책 대여 함수
def rental_book(info):
    name = list(info.keys())[0]  # 딕셔너리는 키로 접근!
    age = list(info.values())[0]
    rental = age // 10
    decrease_book(rental)
    print(f'{name}님이 {rental}권의 책을 대여하였습니다.')

# 회원 수 증가
def increase_user():
    global number_of_people
    number_of_people += 1

# 회원 생성
def create_user(name, age, address):
    increase_user()
    user_info = {
        'name': name,
        'age': age,
        'address': address
    }
    print(f'{name}님 환영합니다!')
    return user_info

# --------실행 부분--------

name = ['김시습', '허균', '남영로', '임제', '박지원']
age = [20, 16, 52, 36, 60]
address = ['서울', '강릉', '조선', '나주', '한성부']

many_user = []
for n, a, ad in zip(name, age, address):
    many_user.append(create_user(n, a, ad))

user_info = []

for user in many_user:
    name = user['name']
    age = user['age']
    new_dict = {name: age}
    user_info.append(new_dict)

for info in user_info:
    rental_book(info)
